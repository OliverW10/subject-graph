{
  "scripts": {
    "dev": "(mkdir -p dist) && (cp subjects.json dist) && (parcel ./index.html)"
  },
  "dependencies": {
    "graphology": "^0.25.4",
    "graphology-layout": "^0.6.1",
    "graphology-layout-force": "^0.2.4",
    "graphology-layout-forceatlas2": "^0.10.1",
    "sigma": "^3.0.0-beta.29"
  },
  "devDependencies": {
    "parcel": "^2.12.0"
  }
}
